{% extends 'base.html.twig' %}

{% block body %}
    <h1>Entrefinance</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entreFinance.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('entrefinance_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('entrefinance_edit', { 'id': entreFinance.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
